------

## 🚀 DevOps 运维工程师 Linux 命令精讲教程

### 一、文件与目录核心操作 (File & Directory Management)

| **命令** | **常用选项**                                                 | **示例及实战注解**                                           |
| -------- | ------------------------------------------------------------ | ------------------------------------------------------------ |
| `ls`     | `-l` (详细列表), `-h` (人类可读), `-a` (显示隐藏文件), `-d` (仅显示目录本身) | `ls -lha /var/log` (查看日志目录详细信息)                    |
| `cd`     | `~` (主目录), `-` (上次目录)                                 | `cd /etc/nginx; cd -` (在两个目录间快速切换)                 |
| `mkdir`  | `-p` (递归创建)                                              | `mkdir -p /data/app/config` (确保父目录存在)                 |
| `cp`     | `-r` (递归复制), `-p` (保留属性), `-i` (交互式覆盖)          | `cp -rp /etc/default/nginx .` (复制配置文件到当前目录并保留时间戳) |
| `mv`     | -                                                            | `mv old_file new_file` (重命名); `mv file.txt /tmp/` (移动文件) |
| `rm`     | `-r` (递归删除), `-f` (强制删除, 不提示), `-i` (交互式提示)  | `rm -rf /data/temp/*` (清除临时目录，**慎用！**)             |
| `ln`     | `-s` (创建软链接/符号链接)                                   | `ln -s /etc/nginx/nginx.conf ~/nginx.conf` (创建配置文件的快捷方式) |
| `which`  | -                                                            | `which java` (查找当前使用的 Java 命令的绝对路径)            |

### 二、文本查看与编辑 (Viewing & Editing Text)

| **命令** | **常用选项**                          | **示例及实战注解**                                           |
| -------- | ------------------------------------- | ------------------------------------------------------------ |
| `cat`    | `-n` (显示行号)                       | `cat script.sh` (快速查看小文件内容)                         |
| `head`   | `-n N` (查看前 N 行)                  | `head -n 20 error.log` (快速查看日志文件的开头部分)          |
| `tail`   | `-f` (实时跟踪), `-n N` (查看后 N 行) | `tail -f /var/log/app.log` (运维最常用，实时监控日志输出)    |
| `less`   | -                                     | `less big_access.log` (分页查看大文件，按 `/` 搜索，按 `q` 退出) |
| `more`   | -                                     | `more config.txt` (简单分页查看，功能不如 `less` 强大)       |
| `vim/vi` | -                                     | `vim /etc/hosts` (编辑配置文件，是运维工程师必备技能)        |

### 三、文本处理与数据过滤 (Text Processing & Filtering)

| **命令** | **常用选项**                                                 | **示例及实战注解**                                           |
| -------- | ------------------------------------------------------------ | ------------------------------------------------------------ |
| `grep`   | `-i` (忽略大小写), `-v` (反向匹配), `-r` (递归搜索), `-E` (支持扩展正则表达式) | `grep -r "TODO" ./src/` (在目录下递归查找代码中的 TODO 注释) |
| `find`   | `-name`, `-type`, `-mtime`, `-exec`                          | `find . -type f -name "*.tmp" -delete` (查找并删除当前目录下所有临时文件) |
| `sed`    | `-i` (原地编辑), `s/old/new/g` (全局替换)                    | `sed -i 's/80/8080/g' nginx.conf` (将配置文件中的 80 端口全部替换为 8080) |
| `awk`    | `{print $N}` (打印第 N 列)                                   | `netstat -tuln | awk '{print $4}'` (结合 `netstat` 打印第四列数据，如本地地址) |

### 四、进程、资源与系统 (Process, Resource & System)

| **命令**   | **常用选项**                                        | **示例及实战注解**                                           |
| ---------- | --------------------------------------------------- | ------------------------------------------------------------ |
| `ps`       | `aux` (显示所有用户进程详细信息), `ef` (树状显示)   | `ps aux | grep app_name` (查找特定应用进程的 PID)            |
| `top/htop` | -                                                   | `top` (实时监控 CPU、内存和进程，按 `P` 按 CPU 排序，按 `M` 按内存排序) |
| `kill`     | `-9` (强制终止), `-15` (正常终止)                   | `kill 12345` (正常终止 PID 为 12345 的进程); `kill -9 12345` (强制杀死) |
| `lsof`     | `-i :PORT`, `-i @IP`, `+D` (查找打开特定端口的进程) | `sudo lsof -i :8080` (查找占用 8080 端口的进程 PID，常用于端口冲突排查) |
| `nohup`    | -                                                   | `nohup ./server.sh > log.out 2>&1 &` (后台运行脚本，并将标准输出和错误输出重定向到 `log.out`) |
| `free`     | `-h` (人类可读格式)                                 | `free -h` (快速查看系统内存和交换空间使用情况)               |
| `df`       | `-h` (人类可读格式), `-T` (显示文件系统类型)        | `df -h` (检查磁盘空间使用率，防止磁盘满载)                   |
| `du`       | `-sh` (汇总并以人类可读格式显示)                    | `du -sh /var/log` (查看日志目录总大小)                       |

### 五、网络与服务管理 (Network & Service Management)

| **命令**       | **常用选项**                                                 | **示例及实战注解**                                           |
| -------------- | ------------------------------------------------------------ | ------------------------------------------------------------ |
| `netstat`      | `-tuln` (TCP/UDP, 监听状态, 不解析主机名/端口号)             | `netstat -tuln | grep 80` (检查 80 端口是否在监听状态)       |
| `ss`           | `-tuln` (功能同 `netstat`，更快)                             | `ss -tuln` (现代 Linux 系统推荐使用 `ss` 替代 `netstat`)     |
| `curl`         | `-I` (只显示头部), `-X` (指定请求方法), `-d` (发送 POST 数据) | `curl -I https://api.example.com` (快速测试 API 连通性和 HTTP 状态码) |
| `systemctl`    | `start`, `stop`, `restart`, `status`, `enable`               | `systemctl status nginx` (查看 Nginx 服务状态，现代 Linux 发行版推荐用法) |
| `firewall-cmd` | `--zone=public`, `--add-port`, `--permanent`                 | `sudo firewall-cmd --add-port=8080/tcp --permanent` (开放 8080 端口，需要 `reload` 生效) |

### 六、打包、传输与同步 (Archive, Transfer & Sync)

| **命令** | **常用选项**                                                 | **示例及实战注解**                                           |
| -------- | ------------------------------------------------------------ | ------------------------------------------------------------ |
| `tar`    | `-czvf` (创建 gz 压缩), `-xzvf` (解压 gz), `-j` (bz2), `-J` (xz) | `tar -czvf app.tar.gz /home/app/` (打包并压缩应用目录)       |
| `unzip`  | `-d` (解压到指定目录)                                        | `unzip archive.zip -d /opt/deploy` (解压 ZIP 文件)           |
| `rz/sz`  | -                                                            | **rz** (在终端中输入，用于从本地上传文件到服务器); **sz file.txt** (用于将服务器文件下载到本地) |
| `rsync`  | `-avz` (归档模式, 详细, 压缩)                                | `rsync -avz local/dir user@remote:/path/` (高效同步文件，只传输变化的部分) |